# Лабораторная работа №4. Эксперимент

## Автор
- ФИО: Ларионов Владислав Васильевич
- ИСУ: 466468
- Группа: P3209

## Вариант
```md
forth | cisc | harv | hw | tick | binary | trap | mem | pstr | prob1 | cache
```

| Опция | Реализация |
| --- | --- |
| `forth` | Минимальный Forth-подобный язык с обратной польской нотацией, процедурами и `execution token`. |
| `cisc` | CISC ISA для стекового процессора: инструкции переменной длины, memory+ALU операция, управление спецрегистрами и `PUSHN`. |
| `harv` | Раздельные command memory и data memory; транслятор формирует отдельные бинарные образы. |
| `hw` | Блок управления проектируется как hardwired-автомат без микрокода. |
| `tick` | Модель процессора должна исполняться с точностью до такта и вести тактовый журнал. |
| `binary` | Машинный код и память данных сериализуются в настоящие бинарные файлы. |
| `trap` | Ввод реализуется через trap-прерывания по расписанию входных токенов; обработчик пишется на Forth. |
| `mem` | Ввод-вывод отображается в адресное пространство data memory. |
| `pstr` | Статические строки хранятся в data memory как Pascal-строки. |
| `prob1` | Обязательная программа: наибольший палиндром, являющийся произведением двух трехзначных чисел. |
| `cache` | Усложнение не было реализовано |

## Язык программирования

Исходный язык проекта -- минимальный Forth-подобный язык с обратной польской нотацией. Программа представляет собой последовательность токенов, которые исполняются слева направо. Основная структура исполнения -- стек данных; большинство слов берут аргументы с вершины стека и кладут результат обратно на стек.

### Синтаксис

Синтаксис описан в EBNF -- расширенной форме Бэкуса-Наура.

```ebnf
program              ::= element*
element              ::= definition
                       | interrupt-definition
                       | variable-definition
                       | constant-definition
                       | statement

definition           ::= ":" identifier statement* ";"
interrupt-definition ::= ":interrupt" identifier statement* ";"
variable-definition  ::= "variable" identifier
constant-definition  ::= literal "constant" identifier

statement            ::= literal
                       | string-literal
                       | word
                       | xt-literal
                       | pushn-literal
                       | addm-literal
                       | if-statement
                       | begin-loop
                       | counted-loop

if-statement         ::= "if" statement* ("else" statement*)? "then"
begin-loop           ::= "begin" statement* "until"
counted-loop         ::= "do" statement* "loop"

xt-literal           ::= "'" identifier
pushn-literal        ::= "pushn" integer integer*
addm-literal         ::= "addm" (identifier | integer)
word                 ::= identifier | builtin-symbol
literal              ::= integer
integer              ::= ["-"] digit+
string-literal       ::= "s" '"' string-char* '"'
identifier           ::= non-space-token
builtin-symbol       ::= "+" | "-" | "*" | "/" | "=" | "<" | ">"

comment              ::= "\" <any characters until end of line>
```

`identifier` здесь означает любой пользовательский токен без пробельных символов, который не является числом, строковым литералом или служебным словом синтаксиса.

Строковый литерал записывается как `s"text"` и компилируется в статическую Pascal-строку в памяти данных: первое машинное слово хранит длину строки, следующие слова -- коды символов.

### Семантика

Вычисления выполняются последовательно слева направо. Числовой литерал кладет значение на стек данных, строковый литерал кладет адрес статической Pascal-строки, а имя встроенного или пользовательского слова вызывает соответствующий код. Операции записываются в обратной польской нотации: выражение `(2 + 3) * 4` записывается как `2 3 + 4 *`.

Токены отделяются пробелами или синтаксисом строк. Строковый литерал `s"Hello"` считается одним токеном, даже если внутри строки есть несколько символов.

Область видимости:

- область видимости слов глобальная;
- пользовательские процедуры, переменные и константы могут использоваться до текстового места определения, потому что транслятор сначала собирает все объявления;
- имена процедур, переменных и констант глобальны;
- статические строки не имеют имён и доступны по адресу, который строковый литерал кладёт на стек.

Переменные и константы:

- `variable name` выделяет одно слово в памяти данных;
- обращение к `name` кладет на стек адрес этой ячейки;
- `N constant name` создает именованную константу;
- обращение к `name` кладет на стек значение `N`.

Execution token:

- значение, которое ссылается на исполняемое слово;
- токен получается словом `'` и выполняется словом `execute`;
- апостроф используется только когда нужно получить ссылку на слово как данные;
- без апострофа имя слова означает обычный вызов этого слова.

Процедуры:

- процедура определяется через `: name ... ;`;
- вызов процедуры сохраняет адрес возврата на стек возвратов процессора;
- после сохранения адреса возврата управление передается телу процедуры;
- `;` компилируется как возврат;
- в определении `:interrupt name ... ;` завершающий `;` компилируется как возврат из обработчика прерывания;
- аргументы и результат процедуры передаются через стек данных.

### Управление потоком

`if ... else ... then` снимает со стека предикат. Ноль означает ложь, любое ненулевое значение -- истину.

`begin ... until` повторяет тело, пока предикат на вершине стека равен нулю. В конце каждой итерации тело должно оставить предикат на стеке.

`do ... loop` -- счетный цикл. Перед `do` на стеке должны лежать `limit start`. Индекс цикла доступен через слово `i`. На каждой итерации индекс увеличивается на 1, цикл выполняется пока `i < limit`.

### Минимальный словарь

| Слово | Стековый эффект | Назначение |
|:------|:----------------|:-----------|
| `+` | `a b -- sum` | Сложение |
| `addm name` | `a -- sum` | Сложить вершину стека со словом памяти данных по адресу |
| `-` | `a b -- diff` | Вычитание |
| `*` | `a b -- prod` | Умножение |
| `/` | `a b -- quot` | Целочисленное деление |
| `mod` | `a b -- rem` | Остаток |
| `=` | `a b -- flag` | Равенство |
| `<` | `a b -- flag` | Меньше |
| `>` | `a b -- flag` | Больше |
| `get-carry` | `-- flag` | Положить на стек флаг переноса `C` |
| `dup` | `a -- a a` | Дублировать значение |
| `drop` | `a --` | Удалить значение |
| `swap` | `a b -- b a` | Поменять значения местами |
| `over` | `a b -- a b a` | Скопировать второе значение |
| `i` | `-- index` | Получить индекс текущего `do ... loop` |
| `load` | `addr -- value` | Прочитать слово из памяти данных |
| `store` | `value addr --` | Записать слово в память данных |
| `write-char` | `char --` | Записать символ в memory-mapped регистр вывода |
| `read-char` | `-- char` | Прочитать символ из memory-mapped регистра ввода |
| `ei` | `--` | Разрешить trap-прерывания |
| `di` | `--` | Запретить trap-прерывания |
| `execute` | `xt --` | Выполнить execution token |
| `pushn k v0 ...` | `-- v0 ...` | Положить на стек `k` значений |
| `halt` | `--` | Остановить процессор |

В описании стековых эффектов правое значение перед `--` считается вершиной стека: в записи `a b -- sum` значение `b` находится на вершине перед выполнением слова.

## Организация памяти

Архитектура памяти -- Harvard:

- память команд: `2^16` ячеек по 8 бит;
- память данных: `2^16` ячеек по 32 бита.

```text
Registers
+------------------------------------------+
| pc ir arg tos nos sp rp tmp mdr flags    |
+------------------------------------------+

Command memory, 8-bit cells
+------------------------------------------+
| 0000 : opcode                            |
| 0001 : operand high / immediate byte     |
| 0002 : operand low                       |
| ...  : procedure code                    |
| ...  : interruption handlers             |
| ...  : main                              |
+------------------------------------------+

Data memory, 32-bit cells
+------------------------------------------+
| 0000 : reserved/null                     |
| 0001 : pstr length / static data         |
| 0002 : pstr char                         |
| ...  : variables                         |
| D000..DFFF : data stack                  |
| E000..EFFF : return stack                |
| FF00, FF02 : memory-mapped I/O           |
+------------------------------------------+
```

Регистры процессора:

| Регистр | Размер | Назначение |
| --- | ---: | --- |
| `pc` | 16 бит | Адрес текущего обрабатываемого байта в памяти команд |
| `ir` | 8 бит | Opcode текущей инструкции |
| `arg` | 32 бита | Буфер операнда, собранного из следующих байтов команды |
| `sp` | 16 бит | Адрес слова на вершине стека данных |
| `rp` | 16 бит | Адрес слова на вершине стека возвратов |
| `tos` | 32 бита | Кэш `DataMem[sp]`, верхушка стека данных |
| `nos` | 32 бита | Кэш `DataMem[sp - 1]`, второй элемент стека данных |
| `tmp` | 32 бита | Временный регистр для развёрнутых стековых операций |
| `mdr` | 32 бита | Регистр данных памяти; любое чтение data memory сначала защёлкивается сюда |
| `flags` | 6 бит | Флаги `Z`, `N`, `C`, `V`, `IE`, `INT` |

Флаги:

- `Z` -- результат равен нулю;
- `N` -- результат отрицательный;
- `C` -- перенос в беззнаковой арифметике;
- `V` -- переполнение в знаковой арифметике;
- `IE` -- прерывания разрешены;
- `INT` -- процессор исполняет обработчик прерывания.

Отображение сущностей языка на память:

- инструкции, процедуры и обработчики `:interrupt` размещаются в памяти команд;
- `execution token` -- байтовый адрес процедуры в памяти команд;
- 16-битные адреса и относительные смещения кодируются двумя байтами после opcode;
- 32-битный числовой литерал кодируется четырьмя байтами после opcode;
- `pstr` занимает `len(string) + 1` слов данных: длина и символы;
- `variable` занимает одно 32-битное слово данных;
- динамическая Pascal-строка может расти в свободной области после статического образа data memory: её первое слово хранит текущую длину, последующие слова -- символы;
- временные значения размещаются на стеке данных;
- отдельные адреса в data memory используются как memory-mapped регистры ввода-вывода.

Отображение портов ввода-вывода:

| Адрес | Назначение |
| --- | --- |
| `0xFF00` | входной символ |
| `0xFF02` | выходной символ |

## Схемы

### DataPath

![DataPath](./schemes/datapath.png)

### ControlUnit

![ControlUnit](./schemes/control_unit.png)

## Модель процессора

Модель `machine.py` исполняет бинарные образы command memory и data memory с точностью до такта. Управление реализовано как hardwired FSM со стадиями:

- `FETCH_OPCODE` -- чтение opcode, защелкивание `ir`, увеличение `pc` на 1;
- `FETCH_ARGUMENT` -- чтение явного операнда нужной длины, защелкивание `arg`, увеличение `pc`;
- `EXECUTE` -- выполнение ALU-операции, защёлкивание спецрегистра, запись памяти или изменение управления;
- `MEM_READ` -- чтение из data memory или MMIO и защёлкивание результата в `mdr`;
- `WRITEBACK` -- использование `mdr` для завершения `RET` или `IRET`;
- `MEMORY_ALU` -- сложение `tos` с ранее прочитанным в `mdr` словом для `ADDM`;
- `CALL_ENTER` -- запись адреса возврата в `DataMem[RP]` и переход к процедуре;
- `CALLXT_LOAD_TOS`, `CALLXT_LATCH_TOS`, `CALLXT_LOAD_NOS`, `CALLXT_LATCH_NOS` -- удаление использованного execution token и восстановление кэша стека перед началом косвенно вызванного слова;
- `FETCH_PUSHN_VALUE` -- чтение очередного 32-битного значения `PUSHN`, защелкивание `arg`, увеличение `pc`;
- `PREPARE_PUSHN_NOS` -- защёлкивание `nos <- tos`;
- `PREPARE_PUSHN_SP` -- вычисление ALU и защёлкивание `sp <- sp + 1`;
- `WRITE_PUSHN_VALUE` -- одновременные `tos <- arg` и `DataMem[sp] <- arg`;
- `INTERRUPT_PREPARE`, `INTERRUPT_ENTER` -- увеличение `rp`, сохранение `pc` в стек возвратов и переход к обработчику.

CLI-интерфейс:

```bash
python3 machine.py <code_file> [data_file] [schedule_file] [limit]
```

`code_file` -- бинарный образ command memory, `data_file` -- бинарный образ data memory, `schedule_file` -- расписание входных токенов для trap-ввода, `limit` -- максимальное число тактов моделирования. Если рядом с `code_file` есть файл `<code_file>.interrupts`, модель автоматически использует его как таблицу адресов обработчиков прерываний.

## Система команд

CISC-свойства:

- обычные инструкции имеют переменную длину: 1, 3 или 5 байтов;
- `ADDM` совмещает арифметику с чтением памяти данных;
- `EI`, `DI` и `GET_CARRY` напрямую работают со специальным регистром `flags`;
- `PUSHN` имеет переменное число аргументов и явно дочитывает дополнительные байты команды.

Память данных однопортовая: за такт выполняется не более одного чтения или одной записи `Data Memory/MMIO`. Чтение всегда сначала защёлкивается в `MDR`. 

### Кодирование

Первый байт каждой инструкции -- opcode. Многобайтные операнды кодируются в big-endian порядке.

| Формат | Длина | Назначение |
| --- | ---: | --- |
| `OP` | 1 байт | безадресная операция со спецрегистрами, ALU или управлением |
| `OP addr16/rel16` | 3 байта | абсолютная или относительная адресация |
| `LDI imm32` | 5 байтов | непосредственное 32-битное значение |
| `PUSHN k v0 ... v{k-1}` | `2 + 4k` байтов | положить на стек переменное число 32-битных значений |

Виды адресации:

| Адресация | Пример | Смысл |
| --- | --- | --- |
| Регистровая неявная | `ADD`, `MOV_TOS_NOS` | Операнды выбираются из `Special Register File` |
| Непосредственная | `LDI 10` | Операнд закодирован в команде |
| Абсолютная | `LOADA 0x0100` | Адрес слова data memory задан в команде |
| Косвенная | `LOAD`, `STORE` | Адрес слова data memory находится в `TOS` |
| Через спецрегистр | `LOAD_SP_M1`, `STORE_RP_TOS` | Адрес вычисляется из `SP/RP` и смещения |
| Относительная | `JZ +12` | Знаковое смещение в байтах относительно `pc` после текущей команды |

### Команды

В именах `MOV_<dst>_<src>` сначала указывается регистр назначения. Например, `MOV_TOS_NOS` выполняет `TOS <- NOS`.

| Инструкция | Opcode | Длина | Адресация | Такты | Семантика |
| --- | ---: | ---: | --- | ---: | --- |
| `HALT` | `0x00` | 1 | неявная | 2 | Остановить модель |
| `ADD` | `0x01` | 1 | регистровая | 2 | `TOS <- NOS + TOS`, обновить `ZNCV` |
| `SUB` | `0x02` | 1 | регистровая | 2 | `TOS <- NOS - TOS`, обновить `ZNCV` |
| `MUL` | `0x03` | 1 | регистровая | 2 | `TOS <- NOS * TOS`, обновить `ZNCV` |
| `DIV` | `0x04` | 1 | регистровая | 2 | `TOS <- NOS / TOS`, обновить `ZNCV` |
| `MOD` | `0x05` | 1 | регистровая | 2 | `TOS <- NOS mod TOS`, обновить `ZNCV` |
| `EQ` | `0x06` | 1 | регистровая | 2 | `TOS <- (NOS = TOS)`, обновить `ZNCV` |
| `LT` | `0x07` | 1 | регистровая | 2 | `TOS <- (NOS < TOS)`, обновить `ZNCV` |
| `GT` | `0x08` | 1 | регистровая | 2 | `TOS <- (NOS > TOS)`, обновить `ZNCV` |
| `MOV_TOS_NOS` | `0x09` | 1 | регистровая | 2 | `TOS <- NOS` |
| `MOV_NOS_TOS` | `0x0A` | 1 | регистровая | 2 | `NOS <- TOS` |
| `MOV_TOS_TMP` | `0x0B` | 1 | регистровая | 2 | `TOS <- TMP` |
| `MOV_TMP_TOS` | `0x0C` | 1 | регистровая | 2 | `TMP <- TOS` |
| `MOV_TMP_NOS` | `0x0D` | 1 | регистровая | 2 | `TMP <- NOS` |
| `MOV_NOS_TMP` | `0x0E` | 1 | регистровая | 2 | `NOS <- TMP` |
| `MOV_TOS_MDR` | `0x0F` | 1 | регистровая | 2 | `TOS <- MDR` |
| `MOV_NOS_MDR` | `0x10` | 1 | регистровая | 2 | `NOS <- MDR` |
| `SP_INC` | `0x11` | 1 | регистровая | 2 | `SP <- SP + 1` через ALU |
| `SP_DEC` | `0x12` | 1 | регистровая | 2 | `SP <- SP - 1` через ALU |
| `RP_INC` | `0x13` | 1 | регистровая | 2 | `RP <- RP + 1` через ALU |
| `RP_DEC` | `0x14` | 1 | регистровая | 2 | `RP <- RP - 1` через ALU |
| `LOAD` | `0x15` | 1 | косвенная | 2 | `MDR <- DataMem[TOS]` |
| `LOAD_SP` | `0x16` | 1 | через `SP` | 2 | `MDR <- DataMem[SP]` |
| `LOAD_SP_M1` | `0x17` | 1 | через `SP-1` | 2 | `MDR <- DataMem[SP - 1]` |
| `LOAD_RP` | `0x18` | 1 | через `RP` | 2 | `MDR <- DataMem[RP]` |
| `STORE` | `0x19` | 1 | косвенная | 2 | `DataMem[TOS] <- NOS` |
| `STORE_SP_TOS` | `0x1A` | 1 | через `SP` | 2 | `DataMem[SP] <- TOS` |
| `STORE_SP_M1_TOS` | `0x1B` | 1 | через `SP-1` | 2 | `DataMem[SP - 1] <- TOS` |
| `STORE_SP_M1_NOS` | `0x1C` | 1 | через `SP-1` | 2 | `DataMem[SP - 1] <- NOS` |
| `STORE_RP_TOS` | `0x1D` | 1 | через `RP` | 2 | `DataMem[RP] <- TOS` |
| `EI` | `0x1E` | 1 | неявная | 2 | `IE <- 1` |
| `DI` | `0x1F` | 1 | неявная | 2 | `IE <- 0` |
| `IRET` | `0x20` | 1 | через `RP` | 3 | `MDR <- DataMem[RP]`, `PC <- MDR`, `RP <- RP - 1`, `INT <- 0` |
| `GET_CARRY` | `0x21` | 1 | регистровая | 2 | `TOS <- C` |
| `CALLXT` | `0x22` | 1 | косвенная | 7 | Сохранить `PC`, выполнить `PC <- TOS`, удалить использованный execution token и восстановить `TOS/NOS` |
| `RET` | `0x23` | 1 | через `RP` | 3 | `MDR <- DataMem[RP]`, `PC <- MDR`, `RP <- RP - 1` |
| `LOADA addr` | `0x24` | 3 | абсолютная | 3 | `MDR <- DataMem[addr]` |
| `STOREA addr` | `0x25` | 3 | абсолютная | 3 | `DataMem[addr] <- TOS` |
| `ADDM addr` | `0x26` | 3 | абсолютная | 4 | `MDR <- DataMem[addr]`, `TOS <- TOS + MDR` |
| `JMP rel` | `0x27` | 3 | относительная | 3 | `PC <- PC + rel` |
| `JZ rel` | `0x28` | 3 | относительная | 3 | Если `TOS = 0`, выполнить `PC <- PC + rel` |
| `CALL rel` | `0x29` | 3 | относительная | 4 | Сохранить `PC` в стеке возвратов, выполнить `PC <- PC + rel` |
| `LDI imm` | `0x2A` | 5 | непосредственная | 3 | `TOS <- imm32` |
| `PUSHN k v0 ...` | `0x2B` | `2+4k` | непосредственная | `3+4k` | Явно загрузить в стек `k` значений |

## Транслятор

Транслятор `translator.py` преобразует Forth-программу в два бинарных образа: память команд и память данных.

Трансляция выполняется в два прохода. Сначала исходный текст разбирается на определения, переменные, константы и top-level код; для процедур и управляющих конструкций создаются внутренние метки. Затем Forth-слова разворачиваются в низкоуровневые инструкции, вычисляются байтовые адреса меток и заполняются относительные смещения переходов. Числовой литерал компилируется как последовательность `SP_INC`, `MOV_NOS_TOS`, `LDI`, `STORE_SP_TOS`; стековые слова также компилируются в явные операции со спецрегистрами и памятью. Исключение -- `pushn`, которое напрямую создаёт одну CISC-инструкцию `PUSHN`.

CLI-интерфейс:

```bash
python3 translator.py <input_file> <target_file>
```

Основные выходные файлы:

- `<target_file>` -- бинарный образ command memory;
- `<target_without_bin>.data.bin` -- бинарный образ data memory, если `target_file` оканчивается на `.bin`;
- `<target_file>.interrupts` -- адреса обработчиков `:interrupt`, нужные модели для trap-ввода.

Дополнительно создаются отладочные файлы:

- `<target_file>.hex` -- человекочитаемый dump команд;
- `<target_without_bin>.data.bin.hex` -- dump data memory;
- `<target_file>.symbols` -- адреса сгенерированных меток и процедур.

Пример запуска инструментальной цепочки:

```bash
python3 translator.py program.fth program.bin
python3 machine.py program.bin program.data.bin input.schedule 20000
```

### Пример трансляции

Исходная программа:

```forth
5 1 +
```

Top-level код автоматически помещается в синтетическую процедуру `main`. Каждый литерал явно записывается в физический стек данных, а `+` вычисляет результат в `TOS`, записывает его в `DataMem[SP - 1]`, уменьшает `SP` и восстанавливает `NOS`.

Сгенерированный dump command memory:

```text
0 - 290001 - CALL 1
3 - 00 - HALT
4 - 11 - SP_INC
5 - 0A - MOV_NOS_TOS
6 - 2A00000005 - LDI 5
11 - 1A - STORE_SP_TOS
12 - 11 - SP_INC
13 - 0A - MOV_NOS_TOS
14 - 2A00000001 - LDI 1
19 - 1A - STORE_SP_TOS
20 - 01 - ADD
21 - 1B - STORE_SP_M1_TOS
22 - 12 - SP_DEC
23 - 17 - LOAD_SP_M1
24 - 10 - MOV_NOS_MDR
25 - 23 - RET
```

Полученные метки:

```text
word:main = 4
```

## Тестирование

Запуск всех тестов:

```bash
python3 -m unittest test_golden -v
```

Golden-кейсы лежат в каталоге [`golden`](./golden). 

| Кейс | Что проверяет |
| --- | --- |
| [`addm.yml`](./golden/addm.yml) | CISC-инструкцию `ADDM`: арифметика со словом data memory |
| [`cat_interrupt.yml`](./golden/cat_interrupt.yml) | потоковое эхо до завершающего `NUL`, trap-ввод, обработчик `:interrupt` и `DI` |
| [`double_precision.yml`](./golden/double_precision.yml) | 64-битное сложение пары `hi32:lo32` |
| [`execution_token.yml`](./golden/execution_token.yml) | execution token и косвенный вызов через `execute` |
| [`hello_user_name.yml`](./golden/hello_user_name.yml) | prompt, ввод имени в динамически растущую Pascal-строку и вывод приветствия |
| [`hello.yml`](./golden/hello.yml) | вывод статической Pascal-строки |
| [`prob1_palindrome.yml`](./golden/prob1_palindrome.yml) | Euler problem 4: наибольший палиндром-произведение двух трехзначных чисел |
| [`pushn.yml`](./golden/pushn.yml) | переменная по длине инструкция `PUSHN` |
| [`sort_three.yml`](./golden/sort_three.yml) | чтение трех цифр через trap и сортировка |
